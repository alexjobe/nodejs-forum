<% include ../partials/header %>

<div class = "container">
    <form action = "/topics/<%= topic_id %>/posts/<%= post_id %>/comments/<%= comment._id %>?_method=PUT" method = "POST">
        <div class = "form-group">
            <textarea class="form-control" rows = "5" name = "comment[content]"><%= comment.content %></textarea>
        </div>
        <div class = "form-group">
            <button class = "btn btn-primary btn-block">Submit</button>
        </div>
    </form>
</div>

<% include ../partials/footer %>